@model Sample.Shared.DTO.DeviceDTO


@{
    ViewData["Title"] = "Delete device";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this device?</h3>
<div>
    <h4>Device</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DevicePlatformId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DevicePlatformId)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-route-accessToken="@Model.appAccessToken">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" asp-route-id="@Model.appAccessToken">Back to List</a>
    </form>
</div>
